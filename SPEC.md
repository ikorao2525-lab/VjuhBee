# SPEC.md — BeeCalc: карманный помощник пчеловода

## 0. Для новой ИИ-сессии (начни отсюда)

> **Если что-то непонятно или неоднозначно — НЕ выдумывай и НЕ догадывайся.
> Спроси пользователя или посмотри в код.** Лучше задать лишний вопрос, чем
> сломать работающее приложение.

Этот файл — **единый источник правды** по проекту (см. §11 «SPEC.md — единый
источник правды»). Он страховка от потери чата/истории: любой ИИ или человек
может открыть его и быстро понять, что делает приложение, как оно устроено и
какие решения приняты.

Коротко о проекте:

- **Что это:** офлайн-помощник пчеловода (Android): калькулятор сиропа,
  календарь работ по месяцам/годам, учёт ульев, QR-коды ульев, офлайн-связка
  пасеки (экспорт/импорт JSON).
- **Ключевой принцип:** offline-first. Никаких обязательных сетевых вызовов.
- **Технологии:** Kotlin 2.2.10, Jetpack Compose (Material 3), MVVM + StateFlow,
  Room 2.8.4 (KSP), Navigation Compose, CameraX, ZXing. Полный стек — §3.
- **Текущая версия:** `0.8.2-alpha`, versionCode 20. Ветка git — `alpha`.
- **Статус:** тестовая сборка для распространения тестерам; APK собирается из flavor `beta` и получает отдельное имя файла.
- **Как собрать/установить на Windows 11:** §3 («Расположение окружения») и §12.
- **Структура кода:** §10. Устаревшие/отменённые решения — §4, §13.

**Уточнить у пользователя, прежде чем менять:**
- модель монетизации и площадки (§4 — сейчас это чаевые без премиума, менялось
  не раз — НЕ возвращай старый «премиум»);
- любые изменения базы данных (миграции) — строго с записью в §7 и §13;
- добавление новых зависимостей — строго с записью в §3;
- поведение нижней панели/навигации (см. §5.4 Дерево навигации).

---

## 1. Миссия

Офлайн-справочник и калькулятор пчеловода: быстрые расчёты на пасеке,
сезонный календарь работ, учёт ульев. Работает без интернета.

Ключевые принципы:
- Offline-first: никаких обязательных сетевых вызовов.
- Быстро достал телефон → посчитал → убрал.
- Минимум вложенности, максимум пользы на одном экране.

---

## 2. Аудитория

Пчеловоды-любители и небольшие пасеки.

Портрет пользователя:
- Часто 40+.
- Работает на улице: солнце, перчатки, грязные руки.
- Телефон может быть не самым новым (Android 9–13).
- Не хочет разбираться в сложных интерфейсах.
- Ценит крупные кнопки, крупный шрифт, высокий контраст.

Главный критерий любого UI-решения:
> «Удобно ли это использовать в перчатках на солнце?»

---

## 3. Стек

### Язык и платформа
- Kotlin 2.2.10 (Compose-компилятор встроен в Kotlin 2.x, отдельная версия не нужна).
- Java: JBR 25 (идёт с Android Studio, отдельный JDK не ставим).
- minSdk 28 (Android 9).
- targetSdk 37, compileSdk 37.
  - Google Play требует targetSdk 36+ для новых приложений с 31.08.2026 — берём 37 с запасом.

### UI
- Jetpack Compose (только Compose, без XML-разметок).
- Compose BOM 2026.02.01.
- Material 3.
- Material Icons Extended (версия из BOM).
- Activity Compose 1.13.0.
- Core KTX 1.19.0.

### Навигация
- Navigation Compose 2.9.8 — используется с v0.1.
- Нижняя навигация (Bottom Navigation Bar), 4 вкладки: Сироп, Календарь, Ульи, Инструменты.

### Архитектура
- MVVM + StateFlow.
- ViewModel добавляются по мере роста логики.
- Логика расчётов отделена от UI (domain-слой).
- Источники данных скрыты за repository abstraction.

### База данных
- Room 2.8.4 — с версии 0.1.1 (редактируемый календарь), таблицы ульев добавятся в v0.2 миграцией.
- Кодогенерация через KSP 2.2.10-2.0.2 (KAPT с Kotlin 2.x устарел; KSP — одна строка в Gradle и быстрее).
- База: beecalc.db, единственные экземпляры базы и репозиториев живут в BeeCalcApp (Application).

### Хранение настроек
- DataStore Preferences 1.1.7 — зарезервировано (позже — свои пропорции, размер шрифта).
  Пока не используется в коде (после отказа от PremiumRepository).

### Асинхронность
- Coroutines — приходят транзитивно с lifecycle/compose; отдельную зависимость добавлять только при необходимости.

### Lifecycle
- Lifecycle Runtime KTX 2.11.0.
- Lifecycle ViewModel Compose 2.11.0 — с v0.1 (первые ViewModel).
- Lifecycle Runtime Compose 2.11.0 — с v0.1 (collectAsStateWithLifecycle).

### Монетизация
- **Добровольные чаевые** через внешний сервис (CloudTips и аналоги) — ссылка
  в экране «О приложении», открывается в браузере. Без in-app billing.
- RuStore Pay SDK / Google Play Billing **не используем** (не нужны для чаевых).
- Зависимость DataStore остаётся в проекте под будущие настройки UI.

### Генерация QR (с v0.4)
- ZXing core 3.5.4 — генерация и декодирование штрих-кодов/QR, офлайн,
  без Google-сервисов (SPEC.md §9, v0.4).
- androidx.print 1.1.0 — системный диалог печати QR (SPEC.md §9, v0.4).
- CameraX 1.6.1 (core/camera2/lifecycle/view) — сканирование QR камерой
  (SPEC.md §9, v0.4), офлайн; декодирование — уже имеющийся ZXing Core.

### Сборка
- Android Gradle Plugin 9.3.1 (Kotlin-поддержка встроена, отдельный kotlin-android плагин не нужен).
- Gradle 9.5 (wrapper).
- Kotlin DSL (build.gradle.kts).
- Version Catalog (gradle/libs.versions.toml).
- Android Studio (последняя стабильная).

### Расположение окружения на Windows 11
Это машина разработчика — пользователь `jukle`, ОС Windows 11. Ниже — где лежат все
инструменты и библиотеки, которыми собирается проект. Полезно, если проект открывает
другая модель/человек: эти пути берутся отсюда, а не ищутся заново.

- **Пользователь/домашний каталог:** `C:\Users\jukle`
- **Android Studio** (сама IDE): `C:\Program Files\Android\Android Studio`
  - **JBR / JDK (Java 25)**: `C:\Program Files\Android\Android Studio\jbr`
    → `JAVA_HOME` = `C:\Program Files\Android\Android Studio\jbr`
    - `javac`/`java`: `...\jbr\bin\javac.exe`, `...\jbr\bin\java.exe`
- **Android SDK:** `C:\Users\jukle\AppData\Local\Android\Sdk`
  → `ANDROID_HOME` = `C:\Users\jukle\AppData\Local\Android\Sdk`
  - **platform-tools / adb**: `C:\Users\jukle\AppData\Local\Android\Sdk\platform-tools\adb.exe`
  - **build-tools / aapt2, zipalign, apksigner**: `C:\Users\jukle\AppData\Local\Android\Sdk\build-tools\<версия>\`
  - **платформы**: `C:\Users\jukle\AppData\Local\Android\Sdk\platforms\`
    (установлены 34 и 37; targetSdk = 37)
- **Gradle (wrapper-дистрибутив 9.5.0):** `C:\Users\jukle\.gradle\wrapper\dists\gradle-9.5.0-bin\bvnork1r7n8i6kp5cnkibsc9q\gradle-9.5.0\bin\gradle.bat`
- **Кеш зависимостей Gradle (maven-артефакты):** `C:\Users\jukle\.gradle\caches\modules-2\files-2.1\`
  (здесь лежат AGP, Kotlin, CameraX, Room, ZXing и др.)
- **Проект:** `D:\cursor\rep1\VjuhBee`
  - локализация SDK в проекте: `D:\cursor\rep1\VjuhBee\local.properties` (`sdk.dir=...`)
- **Сборка из консоли (PowerShell):** перед `gradlew`/`gradle.bat` задать в текущей сессии:
  ```powershell
  $env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
  $env:ANDROID_HOME = "C:\Users\jukle\AppData\Local\Android\Sdk"
  ```
- **Основные команды сборки:**
  ```powershell
  .\gradlew.bat assembleBetaDebug     # debug-сборка (установка на телефон)
  .\gradlew.bat assembleBetaRelease   # релизная (подписывается отдельно)
  ```
- **Установка на подключённый телефон (USB):**
  ```powershell
  & "C:\Users\jukle\AppData\Local\Android\Sdk\platform-tools\adb.exe" install -r app\build\outputs\apk\beta\debug\app-beta-debug.apk
  ```
- **Флавора/каналы (productFlavors):** `play`, `rustore`, `beta` (applicationId with `.beta` суффикс).
- **Примечание про сеть:** серверы `dl.google.com` и `repo.maven.apache.org` могут кратковременно
  обрывать TLS-handshake — при сбое качки зависимостей просто повторить сборку или собрать
  из локального кеша (`--offline`).

### Почему такие версии (история решения)
- Изначально планировались Kotlin 1.9.25 + AGP 8.6 + Gradle 8.7 «для стабильности».
- На машине разработчика единственная JVM — Java 25 (JBR из Android Studio), Gradle 8.x на ней не запускается.
- Из SDK-платформ установлены 34 и 37.
- Поэтому стек приведён к текущему шаблону Android Studio (проверен сборкой на этой машине).
  Это же закрывает требование Google Play по targetSdk 36+.

### Правило зависимостей
- Новая зависимость добавляется только с записью в этот раздел.
- Версии Kotlin и Compose BOM не обновлять без необходимости.
- Если что-то работает — не трогать.

---

## 4. Монетизация и площадки

### Решение от 2026-08-06 (актуальное): чаевые без премиума

**Поворот от модели v0.3 (2026-08-05):** отказались от «премиума за 99 ₽ через RuStore»
с разблокировкой чата и бета-доступа. Причины:
- RuStore Billing с февраля 2026 — только ИП; самозанятому не подключить.
- Обещание чата/беты за деньги — уже не «чистые чаевые», а цифровая польза
  (риск для Google Play и лишняя сложность в продукте).
- Законодательство РФ допускает добровольные чаевые/пожертвования физлицам
  через агрегаторы (CloudTips, Boosty и т.п.) — проще и честнее для BeeCalc.

**Итоговая модель:**
- **Все функции бесплатны навсегда** — калькулятор, календарь, ульи, будущие
  QR, отчёты и т.д. Ничего не режем и не продаём.
- **Чаевые — только добровольная поддержка.** Пользователь переходит по ссылке
  на страницу агрегатора (МИР / СБП), платит сколько хочет, **ничего в приложении
  не получает** — ни значков, ни чата, ни раннего доступа.
- В приложении: блок «Поддержать разработчика» + кнопка «Оставить чаевые»
  → `Intent.ACTION_VIEW` в браузере на `BuildConfig.TIPS_URL`.

**Рекомендуемый сервис:** [CloudTips](https://cloudtips.ru/) (Т-Банк / Cloud,
МИР, СБП, персональная ссылка и QR; в FAQ прямо указано размещение ссылки
в мобильном приложении). Альтернативы: Boosty (разовый донат), DonationAlerts.

**Ссылку вписать** в `app/build.gradle.kts` → `buildConfigField("String", "TIPS_URL", ...)`
для flavors `rustore` и `beta` после регистрации в CloudTips. Пока URL пустой —
в UI текст «ссылка появится после подключения», без кнопки.

### Философия
- Всё для работы на пасеке — бесплатно навсегда.
- Новые идеи: нужно в перчатках на солнце → в базу; остальное — тоже в базу,
  пока не решим иначе отдельно (не через «премиум»).

### Бесплатно (навсегда, всё приложение)
- Калькулятор сиропа.
- Календарь работ (годы, корзина, редактирование, учёт продукции).
- Учёт ульев (осмотры, обработки, история).
- Будущее: QR-коды, привязка работ к ульям, отчёты, бета-сборка.

### Чаевые (не премиум)
- Разовая или любая сумма на усмотрение отправителя.
- **Не привязаны к функциям приложения.**
- Общий чат пчеловодов (MAX) — если появится, **для всех**, не за деньги.
- Бета-сборка (flavor `beta`) — **для всех желающих**, кто установил отдельный APK,
  не «награда за чаевые».

### Решение по площадкам (зафиксировано)
- **RuStore — основной канал распространения в РФ.** В сборке `rustore`:
  кнопка чаевых (внешняя ссылка CloudTips). In-app billing RuStore не нужен.
- **Google Play — бесплатная версия.** Сборка `play`: полный функционал,
  **без кнопки внешней оплаты** (осторожность с модерацией Play: нейтральный
  текст «поддержать можно в версии из RuStore»). Чистый peer-to-peer tip
  в Play теоретически допустим, но проверим отдельно, если понадобится.
- **Product flavors:** `play` / `rustore` / `beta` (см. §9).
  BuildConfig: `STORE_CHANNEL`, `SHOW_TIPS`, `TIPS_URL`, `IS_BETA`.

### Google Play и внешние чаевые
- Политика Payments: нельзя продавать цифровые функции/контент в обход Play Billing.
- **Чистые чаевые без бонусов в приложении** ближе к peer-to-peer tip
  (Play Billing не обязателен). Модерация непредсказуема → в `play`-сборке
  пока **нет CTA на оплату**, только информационная строка.
- В `rustore` / `beta` — явная кнопка «Оставить чаевые» на CloudTips.

### Правила
- Без рекламы.
- Базовые расчёты и учёт не обрезаются никогда.
- За чаевые **ничего не разблокируется** — ни сейчас, ни в будущем
  (иначе снова цифровой товар).

### Устаревшее (архив решения 2026-08-05, не делать)
- PremiumState, PremiumRepository, «премиум за 99 ₽», RuStore Pay SDK,
  чат/beta только для плативших — **удалено в v0.3.1**.

---

## 4.1. Законодательство и налоги РФ (ход мыслей, НЕ юрконсультация)

Запись от 2026-08-05, обновлено **2026-08-06** (модель чаевых). Перед приёмом
денег — проверить у бухгалтера/юриста актуальность.

### Чаевые через CloudTips (текущий путь)
- **ИП для RuStore Billing больше не нужен** — in-app оплату не подключаем.
- CloudTips / Boosty: регистрация физлица или самозанятого, привязка карты,
  персональная ссылка. Комиссия агрегатора (~3–10%, уточнять в договоре сервиса).
- **Самозанятость (НПД):** часто достаточно при регулярных поступлениях;
  4% с физлиц, чеки через «Мой налог». Уточнить, как классифицировать
  переводы с CloudTips (чаевые vs доход от деятельности).
- CloudTips в маркетинге пишет про необложение «чаевых» — для разработчика
  приложения формулировка может отличаться; **не полагаться только на рекламу
  сервиса.**

### RuStore Billing (архив, если когда-нибудь понадобится in-app)
- С **1 февраля 2026** Pay SDK RuStore — только **ИП и юрлица**.
- Сейчас **не планируем** — чаевые идут мимо маркетплейса.

### Чеклист до включения живой ссылки на чаевые
1. Зарегистрироваться в CloudTips (или выбранном сервисе), получить ссылку.
2. Проконсультироваться с бухгалтером: НПД / ИП, чеки, лимиты.
3. Вписать `TIPS_URL` в `app/build.gradle.kts` (flavors `rustore`, `beta`).
4. Собрать и проверить: кнопка открывает страницу оплаты в браузере.
5. Аккаунты RuStore / Google Play — для **бесплатной** публикации приложения
   (merchant Play для РФ фактически бесполезен).

### Риски
- Google Play: в `play`-сборке без CTA на оплату (см. §4).
- Смешение чаевых и «платных функций» — **не делаем** (иначе снова цифровой товар).
- Персональные данные: offline-first — проще 152-ФЗ; облако/аккаунты позже —
  политика конфиденциальности.

---

## 5. Экраны MVP (v0.1)

Нижняя навигация через Navigation Compose, 4 вкладки: Сироп, Календарь, Ульи, Инструменты.

### 5.1. Калькулятор

Назначение: быстрый расчёт сахарного сиропа для подкормки пчёл.

#### Входные данные
- Объём сиропа: слайдер 1–50 л.
- Быстрые кнопки объёма: 5 / 10 / 20 / 30 / 50 л.
  - Кнопки дополняют слайдер, не заменяют его.
  - Нажатие кнопки устанавливает значение слайдера.
  - Активная кнопка визуально выделяется.
- Концентрация: 3 пресета.
  - 1:1 (жидкий)
  - 1.5:1 (средний)
  - 2:1 (густой)
- Пресеты отображаются как крупные кнопки (не выпадающий список).
- Обязательная подпись: «Пропорция: сахар : вода, по весу».

#### Выходные данные
- кг сахара.
- л воды.
- Пометка «Расчёт приблизительный» (обязательно).
- Цифры результата крупные — читаются с метра на солнце.

#### Свои пропорции
- В v0.1 НЕ делаем.
- В коде заложить список пресетов как данные, а не зашитые константы.
- Реализация своих пропорций — v0.1.x или v0.2.
- Если свои пропорции появляются до Room — хранить в DataStore.
- Если после Room — можно хранить в Room.

### 5.2. Календарь

Назначение: сезонный справочник работ на пасеке.

#### Содержимое v0.1 → v0.1.1
- 12 месяцев.
- Список работ внутри каждого месяца.
- Текущий месяц подсвечивается и раскрыт при входе.
- С v0.1.1 данные живут в Room и полностью редактируемые:
  - отметки о выполнении (чекбокс, выполненные — зачёркнуты);
  - редактирование и удаление любых работ (в т.ч. стартовых);
  - добавление своих работ в любой месяц.
- Стартовый список (DefaultCalendarTasks) копируется в базу при первом
  запуске. Сроки — для средней полосы РФ; пользователь сдвигает работы
  под свой регион редактированием. Заметка об этом показана над списком.

#### Годы (с v0.1.2)
- Структура: годы → месяцы → работы. Текущий год и месяц раскрыты при входе.
- У каждого года свой набор работ. При первом запуске в новом году список
  копируется из последнего года со сброшенными отметками (prepareYear).
- Прошлые годы остаются как архив со всеми отметками и правками.

#### Корзина (с v0.1.2)
- «Удалить» — мягкое удаление (isDeleted): работа уезжает в секцию
  «Удалённые работы» внизу календаря.
- Из корзины работу можно вернуть («Вернуть») или стереть окончательно
  («Насовсем», с подтверждением).
- При копировании в новый год работы из корзины не переносятся.

#### Категории работ
Каждая работа имеет категорию. Категории показываются текстом, не только цветом.

Список категорий:
1. Осмотр
2. Кормление
3. Лечение / обработки
4. Уход
5. Медосбор
6. Подготовка к сезону
7. Прочее

В коде категории хранятся как идентификаторы (INSPECTION, FEEDING, TREATMENT, MAINTENANCE, HARVEST, SEASONAL, OTHER), а пользователю показывается русское название.

#### Модель данных работы
Каждая работа содержит:
- id
- month (месяц)
- title (название)
- shortDescription (короткое описание)
- fullDescription (полное описание, опционально)
- category (категория)
- importance (важность, опционально)
- tags (теги, опционально)

#### Фильтры
- В v0.1 фильтров нет.
- Структура данных готова к фильтрам.
- Фильтры появятся позже (по категории, месяцу, важности, тегам).

#### Repository abstraction
- Календарь получает данные через CalendarRepository.
- С v0.1.1 реализация — RoomCalendarRepository (таблица calendar_tasks).
- UI от смены источника не зависит.

#### Что календарю ещё понадобится от Room (позже)
- Напоминания.
- Экспорт календаря.
- Привязка работ к ульям.

### 5.3. Ульи (с v0.2 — полноценный учёт)

- Список ульев: карточки с названием и заметкой, «+ Добавить улей».
- Экран улья (маршрут hive/{id}, вкладка «Ульи» остаётся активной):
  - кнопки «+ Осмотр» и «+ Обработка»;
  - «Изменить» (название/заметка) и «Удалить улей» (с подтверждением,
    история удаляется каскадом);
  - история: осмотры и обработки одним списком, свежие сверху,
    записи можно удалять (с подтверждением).
- Осмотр: дата (по умолчанию сегодня, выбор в календаре), рамки и рамки
  с расплодом (крупные степперы «− / +»), «матку видел», заметка.
- Обработка: дата, препарат (обязательно), доза, заметка.
- Подробности таблиц — §7.

### 5.4. Дерево навигации приложения

Обязательная нижняя панель (4 вкладки, всегда видна, кроме полных экранов):

| Вкладка | Маршрут | Куда ведёт |
|---|---|---|
| Сироп | `calculator` | Экран калькулятора сиропа |
| Календарь | `calendar` | Экран календаря работ |
| Ульи | `hives` | Список ульев |
| Инструменты | `tools` | Быстрый доступ к QR-сканированию и синхронизации |

При выборе вкладки используется стандартная логика bottom-nav:
`popUpTo(startDest){saveState}` + `launchSingleTop` + `restoreState`.

Ниже — что на каждом экране, какие кнопки есть и куда они ведут.
«Вкладка остаётся активной» означает: нижняя панель подсвечивает этот пункт.

```
BeeCalcNavHost (startDestination = "calculator")
│
├─ [Syrop] Вкладка: Сироп
│   └─ route "calculator"  → CalculatorScreen
│       └─ Кнопки «5/10/20/30/50 л» → задают слайдер, расчёт на экране
│          (никаких переходов; commit/пресеты — только в этом экране)
│
├─ [Calendar] Вкладка: Календарь
│   └─ route "calendar"  → CalendarScreen
│       ├─ Раскрываемые «Год → Месяц» → раскрывают/сворачивают список работ
│       ├─ Карточка работы (нажатие) → раскрывает полное описание + кнопки
│       │   ├─ «Изменить» → TaskEditorDialog (редактор работы)
│       │   └─ «Удалить» → работа уходит в корзину
│       ├─ «+ Добавить работу» → TaskEditorDialog (новая работа в этом месяце)
│       └─ Секция «Удалённые работы (%d)» (нажатие) → раскрыть корзину
│           ├─ «Вернуть» → вернуть работу из корзины
│           └─ «Насовсем» → удалить навсегда (с подтверждением)
│
├─ [Hives] Вкладка: Ульи
│   └─ route "hives"  → HivesScreen
│       ├─ Иконка QR (в шапке «Мои ульи») → route "qr-scan"
│       ├─ Карточка улья (нажатие) → route "hive/{id}" (вкладка «Ульи» активна)
│       └─ «+ Добавить улей» → HiveEditorDialog (новый улей)
│
│   └─ route "hive/{id}"  → HiveDetailScreen (вкладка «Ульи» остаётся активной)
│       ├─ «← Назад» → popBackStack (возврат в список ульев)
│       ├─ «+ Осмотр» → InspectionDialog (добавить осмотр в историю)
│       ├─ «+ Обработка» → TreatmentDialog (добавить обработку в историю)
│       ├─ «Изменить» → HiveEditorDialog (правка названия/заметки)
│       ├─ «Удалить улей» → подтверждение → каскадное удаление с историей
│       ├─ «Показать QR» → HiveQrDialog (QR-код улья)
│       │   ├─ «Поделиться» → система (постер QR + название + заметка)
│       │   └─ «Печать» → системный диалог печати
│       ├─ Секция «Привязанные работы (%d)» (от v0.5)
│       │   └─ Строка привязанной работы (нажатие) → LinkedTaskDialog
│       │       (read-only описание работы, НЕ выходим из вкладки «Ульи»)
│       │       ├─ «Отвязать от улья» (с подтверждением) → убирает связь,
│       │       │   работа в календаре остаётся
│       │       └─ «Отмена» → закрыть диалог
│       └─ Секция «История» → карточки осмотров/обработок (удаление с подтверждением)
│
├─ [Tools] Вкладка: Инструменты
│   └─ route "tools"  → ToolsScreen
│       ├─ Карточка «Сканировать QR» → route "qr-scan"
│       └─ Карточка «Синхронизация» → route "sync"
│
├─ Полный экран QR-сканера (без нижней/верхней панели)
│   └─ route "qr-scan"  → QrScannerScreen (своя шапка «← Назад» = popBackStack)
│       └─ Распознан QR:
│           ├─ свой улей (по uuid) → route "hive/{id}" (открыть улей)
│           ├─ чужой улей → AlertDialog «Импортировать?»
│           │   ├─ «Импортировать» → создаёт улей, route "hive/{id}"
│           │   └─ «Отмена» → закрыть диалог
│           └─ мусор/не beecalc:// → просто закрыть сканер
│
├─ Полный экран синхронизации (без нижней/верхней панели)
│   └─ route "sync"  → SyncScreen (своя шапка «← Назад» = popBackStack)
│       ├─ «Экспортировать и отправить» → системный share файла .json
│       ├─ «Сохранить на устройство» → системный диалог выбора места (SAF)
│       └─ «Выбрать файл пасеки» → системный выбор файла → мастер импорта
│           (сводка → конфликты → готово; «Отмена» возвращает к карточкам)
│
└─ route "about"  → AboutScreen (иконка «i» в верхней панели)
    ├─ версия/канал/миссия
    └─ блок чаевых (rushore/beta) с кнопкой «Оставить чаевые» → браузер
```

Примечания по навигации (важно для вкладок):
- Маршрут «hive/{id}» привязан к вкладке «Ульи» — при переходе в улей вкладка
  «Ульи» остаётся подсвеченной и клик по ней возвращает в список.
- От v0.5 переход из «Привязанных работ» **не уводит в Календарь**: открывается
  диалог поверх экрана улья (см. выше), чтобы не сбивать нижнюю панель.
- Полные экраны (`qr-scan`, `sync`, `about`) скрывают нижнюю и верхнюю панели.

---

## 6. Формула сиропа

Рабочая модель. Обязательно проверить по справочникам перед релизом.

### Обозначения
- k = кг сахара на 1 л воды.
  - 1:1 → k = 1.0
  - 1.5:1 → k = 1.5
  - 2:1 → k = 2.0
- V = желаемый объём сиропа в литрах.

### Формулы
```
Объём сиропа ≈ вода_л + 0.6 × сахар_кг
вода = V / (1 + 0.6 × k)
сахар = k × вода
```

### Примеры
Для V = 10 л:
- 1:1 → вода ≈ 6.25 л, сахар ≈ 6.25 кг
- 1.5:1 → вода ≈ 5.26 л, сахар ≈ 7.89 кг
- 2:1 → вода ≈ 4.55 л, сахар ≈ 9.09 кг

### Точность
- Точность ±5%.
- В UI обязательна пометка «Расчёт приблизительный».

### Что проверить по справочникам
- Коэффициент 0.6 (увеличение объёма при растворении сахара).
  Справочно: физически 1 кг растворённого сахара добавляет ~0.62–0.63 л объёма,
  так что 0.6 даёт погрешность в пределах заявленных ±5%.
- Действительно ли пропорции считаются по весу (да, в практике 1:1 и 2:1 — по весу).
- Допустимый диапазон концентраций.
- Понятность формулировок «литры» и «килограммы» для пчеловодов.

---

## 7. База данных (с v0.2)

### Таблицы

#### Hive
- id: Int (PrimaryKey)
- name: String
- note: String
- uuid: String (постоянный публичный id, с v0.4/v6, уникальный индекс)

#### Inspection
- id: Int (PrimaryKey)
- hiveId: Int (ForeignKey → Hive)
- date: Long
- frames: Int
- brood: Int
- queenSeen: Boolean
- note: String

#### Treatment
- id: Int (PrimaryKey)
- hiveId: Int (ForeignKey → Hive)
- date: Long
- medicine: String
- dose: String
- note: String

### Функции v0.2 (сделано)
- CRUD ульев (удаление — каскадом с историей).
- Добавление и удаление осмотров.
- Добавление и удаление обработок.
- История по улью (общий список по дате, свежие сверху).
- База v3 (миграция 2→3): таблицы hives, inspections, treatments,
  внешние ключи с ON DELETE CASCADE, индексы по hiveId.

### Функции v0.4 (сделано)
- Постоянный публичный id улья — **uuid** (миграция базы v5→v6):
  - `uuid TEXT NOT NULL` + уникальный индекс, генерируется UUID v4
    на месте через SQLite `randomblob` для уже существующих ульев;
  - у новых ульев задаётся при создании (`UUID.randomUUID()` в ViewModel);
  - переживает перенос базы на другой телефон, ляжет в основу QR-кодов.
- Генерация и показ QR (ZXing Core), сканирование камерой (CameraX + ZXing Core),
  экран QR улья — сделано.
- **Синхронизация пасеки (офлайн) — v0.4 (сделано):** экспорт/импорт всего
  (ульи + осмотры + обработки + календарь) через JSON-файл; слияние по
  стабильному uuid, конфликты разрешаются пользователем.

> **На обсуждение:** переработать кнопку «О приложении» (сейчас — иконка `i`
> в верхней панели). Возможные варианты: пункт меню, перенос в «Инструменты»
> и т.п. Решается позже, к релизу.

---

## 8. Правила UI

### Размеры и контраст
- Тач-цели от 48dp.
- Шрифт от 16sp.
- Высокий контраст.
- Крупные цифры результатов — читаются с метра на солнце.

### Язык
- Русский язык — основной.
- Все строки выносить в strings.xml для локализации.

### Данные
- Все данные локально.
- Никаких спиннеров «ожидание сети».
- Никаких обязательных сетевых вызовов.

### Элементы управления
- Быстрые кнопки для часто используемых значений.
- Пресеты как крупные кнопки, не выпадающие списки.
- Категории в календаре показываются текстом, не только цветом.
- Минимум вложенности экранов.

### Будущие настройки
- Размер шрифта «на лету» — заложить возможность, не делать в MVP.
- Реализация через тему + DataStore, когда понадобится.

---

## 9. Дорожная карта

### v0.1 — MVP
- Навигация через Navigation Compose, 3 вкладки.
- Калькулятор сиропа: слайдер + быстрые кнопки + 3 пресета.
- Календарь: статичный, с категориями.
- Ульи: заглушка.
- Тема Material 3.
- Тестирование на реальном устройстве.

### v0.1.x или v0.2 — свои пропорции
- Пользовательские пропорции (DataStore).
- Возможно, быстрые пресеты объёма расширить.

### v0.1.1 — редактируемый календарь (сделано)
- Room + KSP, таблица calendar_tasks.
- Отметки выполнения, редактирование, удаление, свои работы.
- Кнопки литров в калькуляторе — в 3 ряда, крупнее.

### v0.1.2 — годы и корзина (сделано)
- База v2 (миграция): колонки year и isDeleted.
- Группировка годы → месяцы → работы; новый год копирует список
  со сбросом отметок, прошлые годы — архив.
- Корзина удалённых работ с возвратом.
- Кнопка «Удалить» рядом с «Изменить» в карточке работы.

### v0.2 — ульи (сделано)
- Таблицы Hive/Inspection/Treatment в существующей базе (миграция v3).
- CRUD ульев, осмотры, обработки, история по улью.
- Возможная полировка позже: показывать «Изменить»/«Удалить» в коротких
  карточках календаря без раскрытия (отложено — рушит единство UI).

### v0.2.1 — мёдооткачка в календаре (сделано)
- Поле «Собрано мёда, л» (миграция базы v4).

### v0.2.2 — полный учёт продукции пасеки (сделано)
Исследование (типичный товарный сбор с улья за сезон, средняя полоса):
- **Мёд** — основной продукт, 25–50 кг; считают и в **кг**, и в **л**
  (плотность ~1.4 кг/л, зависит от сорта → автоконвертацию не делаем,
  оба поля независимы).
- **Пыльца (обножка)** — 1–7 кг с улья при пыльцеуловителе; единица **кг**.
- **Перга** — обычно 1–2 кг (до 10 кг у специалистов); единица **кг**.
- **Прополис** — 100–300 г; единица **г**.
- **Воск** — 0.5–1 кг товарного; единица **кг**.
- **Маточное молочко** — редко у любителей, товарно — граммы;
  поле есть (**г**), но большинство пользователей его не заполнит —
  это нормально.

В UI:
- Раздел «Сбор продукции (необязательно)» в редакторе любой работы
  (не только «Медосбор» — пыльцу/прополис можно собрать в день ухода).
- В карточке работы и в заголовке года — сводка ненулевых продуктов
  через « · » (например: «Мёд: 40 кг · Пыльца: 2 кг · Прополис: 120 г»).
- При копировании в новый год все поля сбора сбрасываются.
- Миграция базы v5 (honeyKg, pollenKg, beeBreadKg, propolisGrams,
  waxKg, royalJellyGrams; honeyLiters уже был в v4).

### v0.3 — чаевые / «О приложении» (ветка `beta`, обновлено 2026-08-06)
- Product flavors: `play` / `rustore` / `beta` (dimension `store`).
  - `play`: SHOW_TIPS=false, TIPS_URL="" (нет CTA оплаты).
  - `rustore`: SHOW_TIPS=true, TIPS_URL — после регистрации CloudTips.
  - `beta`: applicationId `com.vjuhbee.beecalc.beta`, суффикс `-beta`,
    SHOW_TIPS=true, IS_BETA=true.
- BuildConfig: STORE_CHANNEL, SHOW_TIPS, TIPS_URL, IS_BETA.
- Экран «О приложении» (Info в TopAppBar): версия, канал, миссия;
  rustore/beta — блок чаевых + кнопка в браузер (если URL задан);
  play — нейтральная пометка про RuStore.
- **v0.3.0 (устарело):** PremiumRepository, тестовый премиум — удалено.
- **v0.3.1:** модель «чаевые без премиума», без PremiumState.
- versionName 0.3.1, versionCode 4.

Ещё не сделано:
- Публикация в RuStore / Play.

CloudTips подключён: `https://pay.cloudtips.ru/p/99472576` (TIPS_URL в rustore/beta).

### Бета-ветка (процесс, зафиксировано 2026-08-06)
- **Git:** ветка `beta` — новые фичи до стабилизации; `main` — релизное.
- **Сборка:** flavor `beta` → `com.vjuhbee.beecalc.beta`, две иконки на телефоне.
- **Кому:** **всем желающим** — установил бета-APK, получил ранние фичи.
  **Не привязано к чаевым.**
- **Что в бету:** QR-сканер, отчёты, новые формулы и т.п.
- **Готово для беты:** собирается, не ломает офлайн-базу миграциями
  вперёд, можно откатить flavor/фичу.
- **Готово для всех:** проверено на устройстве, без тестовых костылей
  в UI, смержено в `main`, собрано flavor `play` и/или `rustore`.

### v0.4 — QR-коды ульев (сделано)
- [x] Постоянный UUID у каждого улья (не внутренний id базы —
  переживает перенос на новый телефон; миграция базы v5→v6).
- [x] Генерация QR (ZXing Core 3.5.4, офлайн) — `utils/QrUtils.kt`,
  кодирует `beecalc://hive/<uuid>[?name=|note=]`, показ на экране улья
  (кнопка «Показать QR» + диалог с подписью названия/заметки).
- [x] «Поделиться» (Intent.ACTION_SEND, PNG через FileProvider)
  и «Печать» (androidx.print.PrintHelper) в диалоге QR.
- [x] Сканирование камерой (CameraX 1.6.1 + ZXing Core, без Google-сервисов):
  экран QrScannerScreen, кнопка «Сканировать» на вкладке «Ульи»,
  QR `beecalc://hive/<uuid>` открывает экран улья. Первое разрешение — камера.
- [x] Рекомендация в UI: наклейка от 5×5 см, матовая ламинация
  (текст-подсказка в диалоге QR).

### v0.5 — привязка работ к ульям (сделано)
- [x] Привязка работы к ульям: у задачи — список uuid ульев (`linkedHiveUuids`),
  выбор ульев в редакторе работы (чипы-переключатели).
- [x] Многие-ко-многим: одна работа — несколько ульев, один улей — несколько работ.
- [x] В карточке работы в календаре показываются имена привязанных ульев.
- [x] На экране улья — короткий список привязанных работ (все годы, архив для
  отчётности); по нажатию — read-only просмотр работы поверх экрана улья; есть отвязка без удаления работы.
- [x] Привязка переносится при синхронизации пасеки (по uuid ульев).
- Основа для отчётности (реализуется в v0.6).

### v0.6 — отчёты и расширяемый учёт урожая (сделано)
- PDF через встроенный PdfDocument, полностью офлайн; «Поделиться», сохранение на устройство и открытие для печати.
- Периоды: плавающий месяц, квартал, полугодие, год, весь архив и свой диапазон.
- В отчёт попадают работы, осмотры, обработки и позиции урожая.
- Урожай хранится как список `HarvestItem` в отдельной таблице `harvest_items`.
- Виды: мёд, пыльца, перга, прополис, воск, маточное молочко, забрус, мерва, пчелиный яд, подмор, матки, отводки, пчелопакеты.
- Позиции урожая передаются в синхронизации стабильными кодами продукции и единиц; старые JSON без `harvestItems` совместимы.
- Единицы: кг, л (для мёда), г и шт; старые фиксированные поля переносятся миграцией v8→v9.
- Категория календаря переименована с «Медосбор» на «Урожай».
- В редакторе выбор продукции сделан через выпадающее меню; единицы и действия используют русские подписи.
- Отчёты группируют работы, осмотры и обработки по месяцам; кнопки редактора увеличены и контрастно выделены для работы на солнце и в перчатках.

## 9.1 План развития после v0.6

Приоритеты определены по пользе для пасечника, offline-first сценарию и риску для существующей базы.

### Приоритет 1 — полноценные итоги урожая в отчётах

Начать следует с этого этапа. Сейчас отчёт уже показывает позиции урожая, но следующий полезный шаг — сводные итоги:

- итоги по каждому виду продукции;
- раздельные итоги по единицам, без смешивания кг и л;
- итоги по месяцам;
- итоги по выбранному улью и по всей пасеке;
- отдельный блок «Итого» в предпросмотре и PDF;
- проверка, что одна работа, привязанная к нескольким ульям, не создаёт скрытого двойного подсчёта.

Критерий готовности: по одному и тому же диапазону экран и PDF показывают одинаковые суммы, а исторические данные после миграции не меняются.

### Приоритет 2 — профиль и история улья

После итогов добавить на экран улья:

- урожай по годам;
- список работ по месяцам;
- фильтры выполненных и невыполненных работ;
- быстрый переход к отчёту по улью;
- отдельную сводку осмотров, обработок и урожая.

### Приоритет 3 — безопасная синхронизация

Улучшить текущий JSON-обмен без обязательного интернета:

- резервная копия перед импортом;
- проверка повреждённого файла;
- подробная сводка, что будет добавлено или изменено;
- конфликты работ и урожая с выбором версии;
- отмена последнего импорта;
- явная версия формата JSON.

Bluetooth, Wi-Fi и серверную синхронизацию пока не начинать: JSON уже закрывает основной сценарий пересменки и сохраняет offline-first архитектуру.

### Приоритет 4 — подготовка v0.6.0 к релизу

- закрыть найденные ошибки alpha-тестирования;
- перенести только проверенные изменения в beta;
- обновить versionName/versionCode;
- проверить все product flavors;
- собрать release APK и подготовить инструкции для тестеров.

## 9.2 Предложения по развитию интерфейса и расчётов

Ниже сначала записаны предложения разработчика, затем отдельным блоком — идеи пользователя. Все версии относятся к периоду до v1.0.0.

### Предложения разработчика

#### v0.6.1 — сводные итоги урожая

- Блок «Итого за период» в отчёте и PDF.
- Суммы по каждому виду продукции и единице измерения.
- Суммы по месяцам, выбранному улью и всей пасеке.
- Единицы кг и л никогда не смешиваются.
- Проверка, что пасечный итог не удваивает урожай работы, привязанной к нескольким ульям.

#### v0.6.2 — профиль улья и быстрый отчёт (альфа-тестирование)

- Отдельная сводка урожая, осмотров, обработок и работ на экране улья (реализовано в alpha).
- Итоги по годам и месяцам.
- Быстрый переход к отчёту по выбранному улью (реализовано в alpha).
- Фильтры истории: все, выполненные, невыполненные.

#### v0.6.3 — безопасный обмен данными (альфа-тестирование)

- Резервная копия перед импортом.
- Проверка повреждённого или неполного JSON-файла.
- Более подробный план импорта: что добавится, изменится или будет пропущено.
- Версия формата JSON и отмена последнего импорта через копию перед операцией.
- В экспорт добавлены метаданные версии приложения, источника и состава данных.
- Восстановление из существующей копии не создаёт дополнительную резервную копию.
- Сохранять в резервной копии все связи «улей ↔ календарная работа» через UUID.
- Сохранять корзину удалённых объектов, чтобы поддерживать восстановление и синхронизацию.
- Добавить метаданные файла: версия приложения, дата создания, источник и статистика записей.
- В будущем добавить данные пасеки: название, регион, адрес, заметку, ФИО и организацию.
- В будущем добавить настройки официального PDF-шаблона и поля подписи.
- После v0.7.0 включить в резервную копию точные даты работ, сроки, повторяемость и напоминания.
- Настройки интерфейса и последний открытый экран сохранять только после стабилизации основных данных.

#### v0.7.0 — точные даты работ (альфа-тестирование)

Для надёжного контроля сроков одной привязки к месяцу недостаточно. Добавить необязательную точную дату выполнения работы с сохранением совместимости старых задач.

- Старые работы без точной даты остаются без даты и отображаются только по году и месяцу.
- В календаре сохранить группировку по годам и месяцам.
- В редакторе доступен необязательный выбор точной даты; в карточке дата показывается только если задана.
- На основе даты показываются статусы «скоро», «просрочено» и «критично».
- Поле `dueDateMillis` переносится через JSON и старые файлы без него остаются совместимыми.

#### v0.9.0 — подготовка к релизу

- Закрыть ошибки alpha-тестирования.
- Проверить миграции баз и восстановление из JSON.
- Проверить все product flavors.
- Обновить versionName/versionCode.
- Собрать release APK и подготовить инструкции для тестеров.

### Идеи пользователя

#### v0.7.1 — главная страница пасеки (альфа-тестирование)

- При первом запуске добавляется выбор: пустая пасека или демонстрационный набор.
- Демонстрационный набор содержит 4 улья, осмотры, обработки, 6 работ, точные сроки, связи и урожай.
- Демоданные имеют отдельные UUID и не загружаются повторно после выбора.

Добавить отдельную вкладку «Главная» с обзором текущего состояния пасеки. Она должна отвечать на вопрос «что нужно сделать сейчас», а не заменять календарь.

Состав главной страницы:

- отдельная вкладка «Главная» с быстрыми действиями и сводкой пасеки.
- предстоящие работы на ближайшие 14 дней;
- просроченные работы до 14 дней;
- критические работы, просроченные более 14 дней;
- количество ульев;
- краткая информация об урожае за текущий год и выбранный период;
- быстрые действия: добавить работу, открыть ульи, сканировать QR, открыть отчёты;
- предупреждение при отсутствии задач, чтобы пустой экран не выглядел ошибкой.
- Работы без точной даты не включаются в блоки сроков и остаются в календаре по месяцу.

Определение статусов:

- «Скоро» — дата выполнения от сегодня до +14 дней;
- «Просрочено» — дата от -1 до -14 дней;
- «Критично» — дата просрочена более чем на 14 дней.

Эта функция зависит от v0.7.0: без точной даты работа не может корректно попасть в эти группы.

#### v0.8.0 — раздел «Калькуляторы»

- Подготовлена тестовая сборка `BeeCalc_v0.8.0-alpha-test.apk` для распространения тестерам.
- В тестовой инструкции указаны состав изменений, ограничения и способ установки.

- Вкладка «Сироп» переименована в «Калькуляторы».
- Добавлен каталог расчётов с рабочими экранами сахарного сиропа и канди.
- Добавлены планируемые пункты зимних запасов и расчёта банок мёда.
- Добавлен чистый `CandiCalculator` и unit-тесты.

Заменить отдельную нижнюю вкладку «Сироп» на вкладку «Калькуляторы». При открытии показывать список доступных расчётов, а затем отдельный экран выбранного калькулятора. Такой подход сохранит место в нижней панели и позволит добавлять инструменты без новых вкладок.

Первый набор калькуляторов:

- сахарный сироп: пропорции, объём воды и сахара;
- канди/медово-сахарное тесто;
- белковая подкормка или пыльцевые лепёшки;
- расчёт зимних запасов корма;
- оценка мёда и количества банок;
- влажность мёда и риск брожения — только как справочный расчёт по показаниям рефрактометра.

#### v0.8.1 — интерактивные сроки и калькуляторы обработок

- Главная страница позволяет открыть работу в календаре и быстро отметить её выполненной.
- Кнопка выполнения обновляет `isDone`, `updatedAt` и автоматически убирает работу из активных блоков сроков.

Следующий этап калькуляторов обработок и санитарии перенесён в v0.8.2.

Добавить расчёты, которые нужны пчеловоду при обработках и обслуживании:

- разведение муравьиной и молочной кислот;
- дозировка щавелевой кислоты по выбранному методу;
- оценка клещевой нагрузки по результатам смыва;
- рецепты полосок/носителей только при наличии проверенной методики;
- очистка рамок и инвентаря растворами щёлочи или отбеливателя.

Для каждого химического расчёта обязательны предупреждения: расчёт не заменяет этикетку зарегистрированного препарата, инструкцию производителя и местные нормы безопасности.

#### v0.8.2 — сворачиваемые сроки и калькуляторы маток

- Секции сроков на главной странице можно сворачивать.
- Заголовок показывает количество работ; по умолчанию раскрыта только секция «Скоро».

Следующий этап калькуляторов маток и расширения пасеки перенесён в v0.8.3.

- рецепт канди для маток;
- планирование вывода маток;
- расчёт отводков и пчелопакетов;
- обратный календарный план до нужной даты.

### Будущие улучшения PDF-отчётов

Идеи пользователя для отдельного этапа развития отчётности после стабилизации текущего PDF:

1. **Заголовок с логотипом приложения.** Добавить в верхнюю часть отчёта логотип BeeCalc, название приложения, версию отчёта и дату формирования. Логотип должен корректно выглядеть как в цветном PDF, так и при чёрно-белой печати.
2. **QR-код как понятная контрольная сумма отчёта.** Добавить внизу PDF QR-код, по которому BeeCalc сможет проверить, что содержимое отчёта соответствует исходным данным и не изменилось после формирования.
   - Для целевой аудитории выбираем простой сценарий без ключей, сертификатов и технических действий для пользователя.
   - QR содержит версию формата, идентификатор отчёта, дату формирования, идентификатор пасеки/улья и контрольную сумму канонических данных отчёта.
   - Проверять следует канонические данные отчёта, а не бинарный PDF: одинаковый набор работ, урожая, осмотров и обработок должен давать одинаковую контрольную сумму.
   - В приложении нужен понятный экран «Проверить отчёт»: сканирование QR и результат «Отчёт соответствует данным» или «Отчёт изменён/не удалось проверить».
   - Такой уровень подходит для защиты от случайных изменений и большинства бытовых подделок, понятен людям без технических знаний и не требует управления ключами.
   - Это не защита от целенаправленной подделки: тот, кто одновременно изменит отчёт и QR, сможет пересчитать контрольную сумму. Асимметричную подпись и юридически значимую проверку оставляем отдельным необязательным этапом.'
3. **Табличный дизайн.** Переработать внешний вид PDF: таблицы для работ, осмотров, обработок и урожая; повторять заголовки таблиц на новых страницах; учитывать узкие экраны и чёрно-белую печать. Сохранить читаемость при большом количестве строк.
4. **Поля для сдачи отчёта.** Добавить в нижней части последней страницы поля: «ФИО пчеловода», «Пасека/организация», «Дата», «Подпись». Предусмотреть настройку, показывать ли эти поля в пользовательском отчёте.

Дополнительное предложение: добавить выбор шаблона **«Рабочий отчёт» / «Отчёт для организации»**. Первый вариант будет компактным и ориентированным на пасечника, второй — с логотипом, реквизитами, полями подписания и более формальной табличной структурой.
### Порядок реализации

1. v0.6.1 — сводные итоги урожая.
2. v0.6.2 — профиль улья и быстрый отчёт.
3. v0.6.3 — резервная копия и безопасный импорт.
4. v0.7.0 — точные даты работ.
5. v0.7.1 — главная страница.
6. v0.8.0–v0.8.2 — раздел калькуляторов и специализированные расчёты.
7. v0.9.0 — стабилизация и подготовка релиза.
### Правило выбора следующей задачи

Новая функция должна либо уменьшать ручной ввод на пасеке, либо помогать быстро найти историю улья, либо повышать сохранность локальных данных. Крупные сетевые функции откладываются до стабилизации локальной модели.
### v1.0 — релиз
- Google Play — бесплатное приложение (play-сборка).
- RuStore — та же база + кнопка чаевых (rustore-сборка).
- Цена приложения: **0 ₽**; монетизация только добровольные чаевые вне приложения.

### Далее
- iOS (Kotlin Multiplatform), если пойдёт доход.

---

## 10. Структура проекта

Пакет приложения: `com.vjuhbee.beecalc`

```
app/src/main/java/com/vjuhbee/beecalc/
│
├── MainActivity.kt                    // Точка входа
├── BeeCalcApp.kt                      // Application: БД и репозитории
├── BeeCalcNavHost.kt                  // Навигация и нижняя панель
│
├── ui/
│   ├── theme/
│   │   ├── Color.kt                   // Цвета
│   │   ├── Theme.kt                   // Тема Material 3
│   │   └── Type.kt                    // Типографика
│   │
│   ├── calculator/
│   │   ├── CalculatorScreen.kt        // UI калькулятора
│   │   └── CalculatorViewModel.kt     // Логика калькулятора
│   │
│   ├── calendar/
│   │   ├── CalendarScreen.kt          // UI календаря
│   │   └── CalendarViewModel.kt       // Логика календаря
│   │
│   ├── hives/
│   │   ├── HivesScreen.kt             // Список ульев
│   │   └── HiveDetailScreen.kt        // История, осмотры, обработки
│   │
│   └── about/
│       ├── AboutScreen.kt             // О приложении / чаевые
│       └── AboutViewModel.kt
│
├── components/                        // Переиспользуемые UI-элементы
│   └── (добавляются по мере необходимости)
│
├── domain/
│   └── SyrupCalculator.kt             // Чистая логика расчёта сиропа
│
├── data/
│   ├── CalendarRepository.kt          // Данные календаря
│   ├── db/                             // Room: задачи и harvest_items
│   └── sync/                           // JSON экспорт/импорт пасеки
│
├── model/
│   └── Models.kt                      // Модели данных, HarvestItem и справочники продукции
│
└── utils/                             // Утилиты (форматирование и т.п.)
    └── (добавляются по мере необходимости)
```

### Принципы структуры
- Каждый экран в своём пакете.
- Логика расчётов отделена от UI (domain/).
- Данные скрыты за repository abstraction (data/).
- Модели данных отдельно (model/).
- Переиспользуемые компоненты выносятся в components/.
- Navigation отдельно от экранов.

---

## 11. Правила разработки

### Процесс
- Коммит после каждого удачного шага.
- Новая зависимость — только с записью в §3 этого файла.
- Тестирование — на реальном устройстве (vivo iQOO 13),
  APK для alpha/beta устанавливается через adb после подтверждения пользователя.
- Новые исправления и тесты сначала делаются на git-ветке `alpha`; после проверки подтверждённые изменения переносятся в `beta`; в `main` — стабильный релиз.
- Сборки: `./gradlew assembleBetaDebug` (бета),
  `assembleRustoreDebug`, `assemblePlayDebug`.
- Целевая аудитория решений: «удобно ли в перчатках на солнце?».

### SPEC.md — единый источник правды
- Этот файл — страховка на случай потери чата/истории.
- Любое решение, исследование, отказ от идеи, юридический/налоговый
  вывод, смена площадки или модели монетизации — **записывать сюда
  подробно**, с датой и ходом мыслей (не только итогом).
- Дорожная карта и журнал (§9, §13) обновляются в том же коммите,
  что и код версии.

### Версии
- Kotlin 2.2.10, Compose BOM 2026.02.01 — не обновлять без необходимости.
- Если что-то работает — не трогать.

### Контекст разработчика
- Разработчик новичок в Android.
- Учится через вайбкодинг.
- Имеет опыт со старыми языками (Pascal).
- Код должен быть понятным, с минимумом магии.
- Приоритет стабильности над новизной.

---

## 12. Сборка и установка (боевые команды, vivo-специфика)

```powershell
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
$env:ANDROID_HOME = "C:\Users\jukle\AppData\Local\Android\Sdk"
.\gradlew.bat :app:assembleBetaDebug
& "$env:ANDROID_HOME\platform-tools\adb.exe" install -r --no-streaming app\build\outputs\apk\beta\debug\app-beta-debug.apk
# beta applicationId: com.vjuhbee.beecalc.beta
```

Особенности vivo:
- обязателен `--no-streaming`: streamed install портит подпись APK.
- В «Для разработчиков» включена «Установка через USB».

---

## 13. Журнал изменений

- **v0.0** — новый проект VjuhBee (пакет `com.vjuhbee.beecalc`), шаблон Compose,
  version catalog, git. Стек: Kotlin 2.2.10, AGP 9.3.1, Gradle 9.5,
  Compose BOM 2026.02.01, minSdk 28, targetSdk/compileSdk 37.
  Осталось проверить: Hello Android на устройстве, GitHub remote.
- **v0.1** — навигация (Navigation Compose 2.9.8, 3 вкладки), калькулятор сиропа
  (слайдер 1–50 л + быстрые кнопки 5/10/20/30/50 + 3 пресета: жидкий/средний/густой,
  логика в domain/SyrupCalculator), календарь (статичный StaticCalendarRepository,
  36 работ по 12 месяцам, категории текстом, текущий месяц подсвечен и раскрыт),
  заглушка ульев. MVVM: CalculatorViewModel, CalendarViewModel (StateFlow).
  Данные календаря — для средней полосы России.
- **v0.1.1** — календарь переведён на Room 2.8.4 (KSP): отметки выполнения,
  редактирование/удаление любых работ, добавление своих, заметка про
  среднюю полосу и регионы. Кнопки литров в калькуляторе — в 3 ряда
  (5/10, 20/30, 50). Добавлен BeeCalcApp (Application) с базой beecalc.db.
- **v0.1.2** — годовые снимки календаря (миграция базы v2: year, isDeleted):
  новый год копирует работы со сбросом отметок, прошлые годы — архив
  с сохранёнными отметками. Корзина удалённых работ («Вернуть»/«Насовсем»).
  Кнопка «Удалить» в карточке рядом с «Изменить». Пресеты концентрации
  без переноса букв (узкие отступы, maxLines=1).
- **v0.2** — учёт ульев (миграция базы v3): список ульев, экран улья
  с историей, осмотры (дата, рамки, расплод, матка, заметка) и обработки
  (дата, препарат, доза, заметка) через крупные диалоги, удаление записей
  и ульев с подтверждением. Маршрут hive/{id} в Navigation Compose.
- **v0.2.1** — мёдооткачка (миграция базы v4): поле «Собрано мёда, л»,
  сумма за год в заголовке года. Переработан §4: премиум — донат.
- **v0.2.2** — полный учёт продукции (миграция базы v5): мёд кг+л,
  пыльца кг, перга кг, прополис г, воск кг, маточное молочко г;
  сводка в карточке и в заголовке года. Зафиксированы площадки
  (RuStore = донат, Play = бесплатная база), чеклист ИП/налогов (§4.1),
  план бета-ветки для премиум-пользователей, правило «писать в SPEC
  подробно» и «агент не ставит APK на телефон».
- **v0.3 (ветка git `beta`)** — flavors play/rustore/beta, экран «О приложении»,
  versionName 0.3.0. Каркас премиума (PremiumRepository) — **заменён в v0.3.1**.
- **v0.3.1** — модель **чаевые без премиума** (§4 от 2026-08-06): удалены
  PremiumRepository и PremiumState; кнопка «Оставить чаевые» → внешняя ссылка
  (CloudTips, `TIPS_URL` в BuildConfig); бета — для всех, не за деньги;
  обновлены §4.1 (налоги/CloudTips). versionName 0.3.1, versionCode 4.
- **v0.3.2** (ветка `beta`) — фундамент под v0.4: постоянный публичный id
  улья **uuid** (миграция базы v5→v6, уникальный индекс). Старым ульям UUID
  генерируется на месте (SQLite `randomblob`), новым — при создании
  (`UUID.randomUUID()` в HivesViewModel). UUID переживает перенос базы и
  ляжет в основу QR-кодов (в работе).
- **v0.3.3** (ветка `beta`) — генерация QR улья: ZXing Core 3.5.4,
  `utils/QrUtils.kt` (`beecalc://hive/<uuid>`), кнопка «Показать QR» +
  диалог с QR и подсказкой про наклейку 5×5. Собрано: `app-beta-debug.apk`.
- **v0.3.4** (ветка `beta`) — в диалоге QR кнопки «Поделиться»
  (Intent.ACTION_SEND, PNG через FileProvider, `res/xml/file_paths.xml`)
  и «Печать» (androidx.print 1.1.0).
- **v0.3.5** (ветка `beta`) — **сканирование QR камерой** (CameraX 1.6.1 +
  ZXing Core, офлайн): экран QrScannerScreen с PreviewView и ImageAnalysis,
  runtime-запрос разрешения камеры, кнопка «Сканировать» на вкладке «Ульи»;
  QR `beecalc://hive/<uuid>` открывает экран улья по uuid 
  (HiveDao.findHiveByUuid).
- **v0.3.6** (ветка `beta`) — расширение схемы QR до
  `beecalc://hive/<uuid>[?name=|note=]` (URL-encoded) и подпись под QR в
  диалоге: название улья + заметка (одна строка с «…»). Импорт по QR
  планируется в v0.4.
- **v0.3.7** (ветка `beta`) — **импорт улья по QR**: при сканировании кода с
  неизвестным uuid приложение предлагает импортировать улей (название/заметка
  из QR, сохраняется оригинальный uuid) и открывает его (`HiveDao.insertHive`
  теперь возвращает id).
- **v0.3.8** (ветка `beta`) — «Поделиться» и «Печать» выгружают/печатают
  **постер** (QR + название + заметка с переносом строк), а не голый QR.
  Версия в «О приложении» приведена к актуальной (BuildConfig, единый источник
  в `build.gradle.kts`).
- **v0.4.0** (ветка `beta`) — **офлайн-синхронизация пасеки**: миграция базы
  v6→v7 (стабильный uuid + updatedAt у осмотров, обработок и работ календаря),
  экспорт всех данных в JSON (̃SyncRepository/̃SyncSerializer), импорт-мастер
  (выбор файла → сводка → разбор конфликтов по ульям → применение слиянием
  по uuid). Экран «Синхронизация» (кнопка на вкладке «Ульи»).
- **v0.4.1** (ветка `beta`) — UI-уборка: новая вкладка «Инструменты» в нижней
  панели (быстрый доступ к «Сканировать QR» и «Синхронизация»); с экрана
  «Ульи» убраны дублирующие кнопки. Пометка на обсуждение: кнопка
  «О приложении».
- **v0.4.2** (ветка `beta`) — UI: кнопка сканирования QR в шапке «Мои ульи»;
  выровнена метка вкладки «Инструменты» (одна строка); из описания импорта
  убран технический «(по uuid)».
- **v0.4.3** (ветка `beta`) — Синхронизация: добавлена кнопка «Сохранить на
  устройство» — экспорт через системный диалог выбора места (SAF), без
  отправки файла. Метка вкладки «Инструменты» уменьшена (одна строка).
- **v0.4.4** (ветка `beta`) — Синхронизация: после выбора файла импорта
  экран полностью переключается в режим мастера «Импорт пасеки» (сводка →
  конфликты → готово), карточки экспорта скрываются; «Отмена» возвращает
  назад. Показывается как отдельный фокус-поток.
- **v0.5.0** (ветка `beta`) — Привязка работ к ульям: в редакторе работы —
  выбор ульев (чипы); имена привязанных ульев показываются в карточке
  календаря; на экране улья — список привязанных работ (все годы).
  Нажатие открывает read-only диалог с полным описанием и кнопкой
  «Отвязать от улья»; выполненные работы зачёркиваются. Привязка хранится
  по uuid ульев и переносится при синхронизации. Миграция БД v7→v8.
  В alpha/beta также исправлены восстановление работ из корзины при импорте,
  обновление `updatedAt` при смене статуса и добавлены unit-тесты sync.

- **v0.6.0** (ветка `beta`) — отчёты и расширяемый учёт урожая: `HarvestItem`, таблица `harvest_items`, миграция v8→v9, дополнительные виды продукции и единицы, экспорт урожая в JSON/PDF, плавающие и пользовательские периоды отчётов, сохранение PDF и печать.
