# SPEC.md — BeeCalc: карманный помощник пчеловода

## 1. Миссия

Офлайн-справочник и калькулятор пчеловода: быстрые расчёты на пасеке,
сезонный календарь работ, учёт ульев. Работает без интернета.

Ключевые принципы:
- Offline-first: никаких обязательных сетевых вызовов.
- Быстро достал телефон → посчитал → убрал.
- Минимум вложенности, максимум пользы на одном экране.

---

## 2. Аудитория

Пчеловоды-любители и небольшие пасеки.

Портрет пользователя:
- Часто 40+.
- Работает на улице: солнце, перчатки, грязные руки.
- Телефон может быть не самым новым (Android 9–13).
- Не хочет разбираться в сложных интерфейсах.
- Ценит крупные кнопки, крупный шрифт, высокий контраст.

Главный критерий любого UI-решения:
> «Удобно ли это использовать в перчатках на солнце?»

---

## 3. Стек

### Язык и платформа
- Kotlin 2.2.10 (Compose-компилятор встроен в Kotlin 2.x, отдельная версия не нужна).
- Java: JBR 25 (идёт с Android Studio, отдельный JDK не ставим).
- minSdk 28 (Android 9).
- targetSdk 37, compileSdk 37.
  - Google Play требует targetSdk 36+ для новых приложений с 31.08.2026 — берём 37 с запасом.

### UI
- Jetpack Compose (только Compose, без XML-разметок).
- Compose BOM 2026.02.01.
- Material 3.
- Material Icons Extended (версия из BOM).
- Activity Compose 1.13.0.
- Core KTX 1.19.0.

### Навигация
- Navigation Compose 2.9.8 — используется с v0.1.
- Нижняя навигация (Bottom Navigation Bar), 3 вкладки.

### Архитектура
- MVVM + StateFlow.
- ViewModel добавляются по мере роста логики.
- Логика расчётов отделена от UI (domain-слой).
- Источники данных скрыты за repository abstraction.

### База данных
- Room 2.8.4 — с версии 0.1.1 (редактируемый календарь), таблицы ульев добавятся в v0.2 миграцией.
- Кодогенерация через KSP 2.2.10-2.0.2 (KAPT с Kotlin 2.x устарел; KSP — одна строка в Gradle и быстрее).
- База: beecalc.db, единственные экземпляры базы и репозиториев живут в BeeCalcApp (Application).

### Хранение настроек
- DataStore — для пользовательских настроек (свои пропорции, размер шрифта и т.п.), когда понадобится.

### Асинхронность
- Coroutines — приходят транзитивно с lifecycle/compose; отдельную зависимость добавлять только при необходимости.

### Lifecycle
- Lifecycle Runtime KTX 2.11.0.
- Lifecycle ViewModel Compose 2.11.0 — с v0.1 (первые ViewModel).
- Lifecycle Runtime Compose 2.11.0 — с v0.1 (collectAsStateWithLifecycle).

### Монетизация
- Play Billing Library 8+ — с версии 0.3.
  - Версии 6 и 7 больше не принимаются Google Play (у billing-библиотек двухлетний цикл:
    v6 закрыта с 31.08.2025, v7 — с 31.08.2026). Брать актуальную 8.x/9.x на момент v0.3.

### Сборка
- Android Gradle Plugin 9.3.1 (Kotlin-поддержка встроена, отдельный kotlin-android плагин не нужен).
- Gradle 9.5 (wrapper).
- Kotlin DSL (build.gradle.kts).
- Version Catalog (gradle/libs.versions.toml).
- Android Studio (последняя стабильная).

### Почему такие версии (история решения)
- Изначально планировались Kotlin 1.9.25 + AGP 8.6 + Gradle 8.7 «для стабильности».
- На машине разработчика единственная JVM — Java 25 (JBR из Android Studio), Gradle 8.x на ней не запускается.
- Из SDK-платформ установлены 34 и 37.
- Поэтому стек приведён к текущему шаблону Android Studio (проверен сборкой на этой машине).
  Это же закрывает требование Google Play по targetSdk 36+.

### Правило зависимостей
- Новая зависимость добавляется только с записью в этот раздел.
- Версии Kotlin и Compose BOM не обновлять без необходимости.
- Если что-то работает — не трогать.

---

## 4. Монетизация (мягкая)

### Бесплатно (навсегда)
- Калькулятор сиропа (базовые расчёты не обрезаются никогда).
- Календарь работ.

### Премиум (разовая покупка 99 ₽, НЕ подписка)
- Учёт ульев.
- Экспорт заметок.
- Полный справочник.

### Площадки и оплата (важно)
- Прямая оплата в Google Play российскими картами не работает с 2022 года
  (МИР/Visa/Mastercard РФ не принимаются; оплата со счёта оператора отключена с апреля 2026).
- Целевая аудитория — русскоязычная, значит премиум через Play Billing ей фактически недоступен.
- Рабочий вариант для оплаты из РФ — RuStore (МИР и СБП работают, есть свой billing SDK).
- Решение по площадкам (Google Play / RuStore / обе) зафиксировать до начала работ по v0.3.

### Правила
- Без рекламы в MVP.
- Базовые расчёты не обрезаются никогда.
- Премиум не блокирует то, что уже было бесплатно.

### Тестирование премиума
- Отдельная тестовая сборка с флагом, без Play Billing.
- В v0.1 никаких экранов премиума не делаем.
- PremiumState в коде: free / premium (заложить модель, не UI).

---

## 5. Экраны MVP (v0.1)

Нижняя навигация через Navigation Compose, 3 вкладки:

### 5.1. Калькулятор

Назначение: быстрый расчёт сахарного сиропа для подкормки пчёл.

#### Входные данные
- Объём сиропа: слайдер 1–50 л.
- Быстрые кнопки объёма: 5 / 10 / 20 / 30 / 50 л.
  - Кнопки дополняют слайдер, не заменяют его.
  - Нажатие кнопки устанавливает значение слайдера.
  - Активная кнопка визуально выделяется.
- Концентрация: 3 пресета.
  - 1:1 (жидкий)
  - 1.5:1 (средний)
  - 2:1 (густой)
- Пресеты отображаются как крупные кнопки (не выпадающий список).
- Обязательная подпись: «Пропорция: сахар : вода, по весу».

#### Выходные данные
- кг сахара.
- л воды.
- Пометка «Расчёт приблизительный» (обязательно).
- Цифры результата крупные — читаются с метра на солнце.

#### Свои пропорции
- В v0.1 НЕ делаем.
- В коде заложить список пресетов как данные, а не зашитые константы.
- Реализация своих пропорций — v0.1.x или v0.2.
- Если свои пропорции появляются до Room — хранить в DataStore.
- Если после Room — можно хранить в Room.

### 5.2. Календарь

Назначение: сезонный справочник работ на пасеке.

#### Содержимое v0.1 → v0.1.1
- 12 месяцев.
- Список работ внутри каждого месяца.
- Текущий месяц подсвечивается и раскрыт при входе.
- С v0.1.1 данные живут в Room и полностью редактируемые:
  - отметки о выполнении (чекбокс, выполненные — зачёркнуты);
  - редактирование и удаление любых работ (в т.ч. стартовых);
  - добавление своих работ в любой месяц.
- Стартовый список (DefaultCalendarTasks) копируется в базу при первом
  запуске. Сроки — для средней полосы РФ; пользователь сдвигает работы
  под свой регион редактированием. Заметка об этом показана над списком.

#### Категории работ
Каждая работа имеет категорию. Категории показываются текстом, не только цветом.

Список категорий:
1. Осмотр
2. Кормление
3. Лечение / обработки
4. Уход
5. Медосбор
6. Подготовка к сезону
7. Прочее

В коде категории хранятся как идентификаторы (INSPECTION, FEEDING, TREATMENT, MAINTENANCE, HARVEST, SEASONAL, OTHER), а пользователю показывается русское название.

#### Модель данных работы
Каждая работа содержит:
- id
- month (месяц)
- title (название)
- shortDescription (короткое описание)
- fullDescription (полное описание, опционально)
- category (категория)
- importance (важность, опционально)
- tags (теги, опционально)

#### Фильтры
- В v0.1 фильтров нет.
- Структура данных готова к фильтрам.
- Фильтры появятся позже (по категории, месяцу, важности, тегам).

#### Repository abstraction
- Календарь получает данные через CalendarRepository.
- С v0.1.1 реализация — RoomCalendarRepository (таблица calendar_tasks).
- UI от смены источника не зависит.

#### Что календарю ещё понадобится от Room (позже)
- Напоминания.
- Экспорт календаря.
- Привязка работ к ульям.

### 5.3. Ульи

- В v0.1: заглушка «Скоро».
- Текст: «Учёт ульев, осмотры и обработки появятся позже».
- Без Room, без CRUD.
- В v0.2: полноценный учёт (см. §7).

---

## 6. Формула сиропа

Рабочая модель. Обязательно проверить по справочникам перед релизом.

### Обозначения
- k = кг сахара на 1 л воды.
  - 1:1 → k = 1.0
  - 1.5:1 → k = 1.5
  - 2:1 → k = 2.0
- V = желаемый объём сиропа в литрах.

### Формулы
```
Объём сиропа ≈ вода_л + 0.6 × сахар_кг
вода = V / (1 + 0.6 × k)
сахар = k × вода
```

### Примеры
Для V = 10 л:
- 1:1 → вода ≈ 6.25 л, сахар ≈ 6.25 кг
- 1.5:1 → вода ≈ 5.26 л, сахар ≈ 7.89 кг
- 2:1 → вода ≈ 4.55 л, сахар ≈ 9.09 кг

### Точность
- Точность ±5%.
- В UI обязательна пометка «Расчёт приблизительный».

### Что проверить по справочникам
- Коэффициент 0.6 (увеличение объёма при растворении сахара).
  Справочно: физически 1 кг растворённого сахара добавляет ~0.62–0.63 л объёма,
  так что 0.6 даёт погрешность в пределах заявленных ±5%.
- Действительно ли пропорции считаются по весу (да, в практике 1:1 и 2:1 — по весу).
- Допустимый диапазон концентраций.
- Понятность формулировок «литры» и «килограммы» для пчеловодов.

---

## 7. База данных (с v0.2)

### Таблицы

#### Hive
- id: Int (PrimaryKey)
- name: String
- note: String

#### Inspection
- id: Int (PrimaryKey)
- hiveId: Int (ForeignKey → Hive)
- date: Long
- frames: Int
- brood: Int
- queenSeen: Boolean
- note: String

#### Treatment
- id: Int (PrimaryKey)
- hiveId: Int (ForeignKey → Hive)
- date: Long
- medicine: String
- dose: String
- note: String

### Функции v0.2
- CRUD ульев.
- Добавление осмотров.
- Добавление обработок.
- История по улью.

---

## 8. Правила UI

### Размеры и контраст
- Тач-цели от 48dp.
- Шрифт от 16sp.
- Высокий контраст.
- Крупные цифры результатов — читаются с метра на солнце.

### Язык
- Русский язык — основной.
- Все строки выносить в strings.xml для локализации.

### Данные
- Все данные локально.
- Никаких спиннеров «ожидание сети».
- Никаких обязательных сетевых вызовов.

### Элементы управления
- Быстрые кнопки для часто используемых значений.
- Пресеты как крупные кнопки, не выпадающие списки.
- Категории в календаре показываются текстом, не только цветом.
- Минимум вложенности экранов.

### Будущие настройки
- Размер шрифта «на лету» — заложить возможность, не делать в MVP.
- Реализация через тему + DataStore, когда понадобится.

---

## 9. Дорожная карта

### v0.1 — MVP
- Навигация через Navigation Compose, 3 вкладки.
- Калькулятор сиропа: слайдер + быстрые кнопки + 3 пресета.
- Календарь: статичный, с категориями.
- Ульи: заглушка.
- Тема Material 3.
- Тестирование на реальном устройстве.

### v0.1.x или v0.2 — свои пропорции
- Пользовательские пропорции (DataStore).
- Возможно, быстрые пресеты объёма расширить.

### v0.1.1 — редактируемый календарь (сделано)
- Room + KSP, таблица calendar_tasks.
- Отметки выполнения, редактирование, удаление, свои работы.
- Кнопки литров в калькуляторе — в 3 ряда, крупнее.

### v0.2 — ульи
- Таблицы Hive/Inspection/Treatment в существующей базе (миграция).
- CRUD ульев.
- Осмотры.
- Обработки.
- История по улью.

### v0.3 — премиум
- Решение по площадкам: Google Play / RuStore / обе (см. §4 «Площадки и оплата»).
- Play Billing Library 8+ (если Google Play) и/или RuStore billing SDK.
- Разовая покупка 99 ₽.
- Экспорт заметок.
- Полный справочник.
- Отдельная тестовая сборка с флагом премиума.

### v1.0 — релиз
- Google Play (бесплатная часть доступна везде; оплата из РФ там не работает).
- RuStore — основной канал платного премиума для РФ, если подтвердим в v0.3.
- Цена 0 ₽ + премиум 99 ₽.

### Далее
- iOS (Kotlin Multiplatform), если пойдёт доход.

---

## 10. Структура проекта

Пакет приложения: `com.vjuhbee.beecalc`

```
app/src/main/java/com/vjuhbee/beecalc/
│
├── MainActivity.kt                    // Точка входа
├── BeeCalcNavHost.kt                  // Навигация, вкладки (с v0.1)
│
├── ui/
│   ├── theme/
│   │   ├── Color.kt                   // Цвета
│   │   ├── Theme.kt                   // Тема Material 3
│   │   └── Type.kt                    // Типографика
│   │
│   ├── calculator/
│   │   ├── CalculatorScreen.kt        // UI калькулятора
│   │   └── CalculatorViewModel.kt     // Логика калькулятора
│   │
│   ├── calendar/
│   │   ├── CalendarScreen.kt          // UI календаря
│   │   └── CalendarViewModel.kt       // Логика календаря
│   │
│   └── hives/
│       └── HivesScreen.kt             // Заглушка ульев
│
├── components/                        // Переиспользуемые UI-элементы
│   └── (добавляются по мере необходимости)
│
├── domain/
│   └── SyrupCalculator.kt             // Чистая логика расчёта сиропа
│
├── data/
│   └── CalendarRepository.kt          // Данные календаря
│
├── model/
│   └── Models.kt                      // Модели данных
│
└── utils/                             // Утилиты (форматирование и т.п.)
    └── (добавляются по мере необходимости)
```

### Принципы структуры
- Каждый экран в своём пакете.
- Логика расчётов отделена от UI (domain/).
- Данные скрыты за repository abstraction (data/).
- Модели данных отдельно (model/).
- Переиспользуемые компоненты выносятся в components/.
- Navigation отдельно от экранов.

---

## 11. Правила разработки

### Процесс
- Коммит после каждого удачного шага.
- Новая зависимость — только с записью в §3 этого файла.
- Тестирование — на реальном устройстве (vivo iQOO 13).
- Целевая аудитория решений: «удобно ли в перчатках на солнце?».

### Версии
- Kotlin 2.2.10, Compose BOM 2026.02.01 — не обновлять без необходимости.
- Если что-то работает — не трогать.

### Контекст разработчика
- Разработчик новичок в Android.
- Учится через вайбкодинг.
- Имеет опыт со старыми языками (Pascal).
- Код должен быть понятным, с минимумом магии.
- Приоритет стабильности над новизной.

---

## 12. Сборка и установка (боевые команды, vivo-специфика)

```bash
./gradlew assembleDebug
adb install -r --no-streaming app/build/outputs/apk/debug/app-debug.apk
adb shell monkey -p com.vjuhbee.beecalc -c android.intent.category.LAUNCHER 1
```

Особенности vivo:
- обязателен `--no-streaming`: streamed install портит подпись APK.
- В «Для разработчиков» включена «Установка через USB».

---

## 13. Журнал изменений

- **v0.0** — новый проект VjuhBee (пакет `com.vjuhbee.beecalc`), шаблон Compose,
  version catalog, git. Стек: Kotlin 2.2.10, AGP 9.3.1, Gradle 9.5,
  Compose BOM 2026.02.01, minSdk 28, targetSdk/compileSdk 37.
  Осталось проверить: Hello Android на устройстве, GitHub remote.
- **v0.1** — навигация (Navigation Compose 2.9.8, 3 вкладки), калькулятор сиропа
  (слайдер 1–50 л + быстрые кнопки 5/10/20/30/50 + 3 пресета: жидкий/средний/густой,
  логика в domain/SyrupCalculator), календарь (статичный StaticCalendarRepository,
  36 работ по 12 месяцам, категории текстом, текущий месяц подсвечен и раскрыт),
  заглушка ульев. MVVM: CalculatorViewModel, CalendarViewModel (StateFlow).
  Данные календаря — для средней полосы России.
- **v0.1.1** — календарь переведён на Room 2.8.4 (KSP): отметки выполнения,
  редактирование/удаление любых работ, добавление своих, заметка про
  среднюю полосу и регионы. Кнопки литров в калькуляторе — в 3 ряда
  (5/10, 20/30, 50). Добавлен BeeCalcApp (Application) с базой beecalc.db.
